spring.application.name=recipe

# --- Use environment variables for DB credentials ---
spring.datasource.url=jdbc:mysql://shinkansen.proxy.rlwy.net:10952/railway?useSSL=false&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=qVpmXPDtsNOpcezJVrPleMtHSImKLMzT

# --- JPA & Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Load data.sql ---
spring.sql.init.mode=always
spring.datasource.initialization-mode=always
spring.jpa.defer-datasource-initialization=true

# --- Show error messages in response (debugging) ---
server.error.include-message=always
server.error.include-binding-errors=always
